import React from 'react';

const MakePostButton = ({openModal}) => {

    return (
        <button
            onClick={openModal}
        >
            button
        </button>
    )
}

export default MakePostButton;